#include <stdio.h>
int main(a,b, c, d, e, f){


  size_in_bit();




}
int size_in_bit(){

  int a;
    float b;
    double c;
    char d;
    short e;
    long f;

    printf(" The size of integer is :%ld ",sizeof(a));
    printf("Th size of float is: %ld",sizeof(b));
 
    printf("The size of double is: %ld",sizeof(c));
  
    printf("The size of char is: %ld",sizeof(d));
    printf("The size of short is : %ld",sizeof(e));
    printf("The size of long is : %ld",sizeof(f));
  
}